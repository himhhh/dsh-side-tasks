{
  "name": "dsh-side-tasks",
  "description": "Codex-style side tasks for the DSH Web GUI: each side task is a branch of the current conversation (forked session with full inherited context) shown as a sidebar tab in dsh-better-sidebar, with chat, cancel, and permanent deletion on close. Requires dsh-better-sidebar.",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./client": "./src/client.js",
    "./package.json": "./package.json"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "inject": [
        "@deepseek-ai/dsh-client-runtime",
        "@deepseek-ai/dsh-client-connection"
      ],
      "platform": "web"
    }
  },
  "peerDependencies": {
    "dsh-better-sidebar": ">=0.12.0"
  },
  "files": [
    "src",
    "cordis.patch.yml",
    "README.md"
  ],
  "license": "MIT"
}
